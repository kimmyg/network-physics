function Projectile( x, y, vx, vy ) {
	this.x = x;
	this.y = y;
	this.vx = vx;
	this.vy = vy;
}

Projectile.prototype.update = function( dt ) {
	this.x += this.vx * dt;
	this.y += this.vy * dt;
}

exports.Projectile = Projectile;
